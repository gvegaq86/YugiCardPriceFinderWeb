import { FiltersBase } from './filters-base';

export class TextboxFilters extends FiltersBase<string> {
  controlType = 'textbox';
  type: string;

  constructor(options: {} = {}) {
    super(options);
    this.type = options['type'] || '';
  }
}
